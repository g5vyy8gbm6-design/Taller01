//sumar los elementos de un arreglo de tamaño N, los elementos son aleatorios 
let arreglo = [1, 20, 3, 4, 50];
let suma = 0;

for (let i = 0; i < arreglo.length; i++) {
    suma += arreglo[i];
}

console.log("La suma de los elementos es: " + suma);
    